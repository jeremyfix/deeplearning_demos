[project]
name = "dlserver"
version = "0.0.1"

dependencies = [
    "PyYAML>=6.0",
    "opencv-python-headless",
    "numpy",
    "onnxruntime"
]

[tool.setuptools]
packages = ["dlserver"]

[project.scripts]
dlserver = "dlserver.serve:main"
